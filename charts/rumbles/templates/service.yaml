apiVersion: v1
kind: Service
metadata:
  name: {{ include "rumbles.fullname" . }}
  labels:
    {{- include "rumbles.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "rumbles.selectorLabels" . | nindent 4 }}
